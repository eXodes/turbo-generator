import { type NextRequest, NextResponse } from "next/server";

const {{ constantCase method }} = async (request: NextRequest): Promise<NextResponse> => {
    /* add api implementation */

    return NextResponse.json({
        /* add json data */
    });
};

export { {{ constantCase method }} };
