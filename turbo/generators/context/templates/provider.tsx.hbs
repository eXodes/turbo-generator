import { createContext, type JSX, type ReactNode } from "react"

type {{ pascalCase name }}ContextType = Record<string, unknown>

const {{ pascalCase name }}Context = createContext({} as {{ pascalCase name }}ContextType)

interface {{pascalCase name}}ProviderProps {
    context: {{ pascalCase name }}ContextType;
    children: ReactNode
}

const {{ pascalCase name }}ContextProvider = ({ context, children }: {{ pascalCase name }}ProviderProps): JSX.Element => {
    return (
        <{{ pascalCase name }}Context.Provider value={context}>{children}</{{ pascalCase name }}Context.Provider>
    )
};

export {
    type {{ pascalCase name }}ContextType,
    {{ pascalCase name }}Context,
    {{ pascalCase name }}ContextProvider
}
